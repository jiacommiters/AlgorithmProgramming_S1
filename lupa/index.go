package main

import "fmt"

func main() {
	// var x, y, z int
	// fmt.Scan(&x, &y, &z)

	// temp := y
	// y = x
	// x = z
	// z = temp

	// fmt.Print(x, y, z)

	// var x float64
	// var y float64

	// y = (3 * x - 5) * (2 * x + 1)

	// fmt.Print(y)

	// var masukan int
	// fmt.Scan(&masukan)

	// angkaPertama := masukan / 1000
	// angkaTerakhir := masukan % 10

	// keluaran := angkaPertama == angkaTerakhir
	// fmt.Println(keluaran)

	// var a, b, c, d, hasil int
	// fmt.Scan(&a, &b, &c, &d, &hasil)
	// a = 1
	// b = 2
	// c = 3
	// d = 4

	// hasil = a + b + c + d
	// fmt.Print(hasil)

	// var r float64
	// fmt.Scan(&r)

	// r = 10.00

	// pi := 3.14
	// volume := (4.0 / 3.0) * pi * r * r * r
	// fmt.Print(volume)

	// var b1, b2, b3, totalGram, kg, gram int
	// fmt.Scan(&b1, &b2, &b3, &totalGram, &kg, &gram)

	// b1 = 6000
	// b2 = 7000
	// b3 = 2000

	// totalGram = b1 + b2 + b3
	// kg = totalGram / 1000
	// gram = totalGram % 1000

	// fmt.Print(kg, gram)

	// var a, b int
	// var hasil bool

	// fmt.Scan(&a, &b)

	// if a == 0 {
	//     hasil = false
	// } else {
	//     hasil = b % a == 0
	// }

	// fmt.Println(hasil)

	// var a, b int
	// fmt.Scan(&a, &b)

	// a, b = b, a
	// fmt.Print(a, b)

	// var bilangan int
	// var hasil bool
	// fmt.Scan(&bilangan, &hasil)

	// if bilangan%2 == 0 {
	// 	hasil = true
	// } else {
	// 	hasil = true
	// }

	// fmt.Print(hasil)

	// var adik, kakak int
	// fmt.Scan(&adik, &kakak)

	// if adik == kakak || kakak-1 == adik || kakak-5 == adik {
	// 	fmt.Print(true)
	// } else {
	// 	fmt.Print(false)
	// }

	// var kartu bool
    // var belanja int
    
    // fmt.Scan(&kartu, &belanja)
    
    // cashback := kartu && belanja >= 1000000
    // fmt.Print(cashback)

	//var b1, b2, b3, totalGram, kg, gram int
	//fmt.Scan(&b1, &b2, &b3, &totalGram, &kg, &gram)
	//b1 = 6000
	//b2 = 7000
	//b3 = 2000
	//totalGram = b1 + b2 + b3
	//kg = totalGram / 1000
	//gram = totalGram % 1000
	//fmt.Print(kg, gram)

	var x, y float64
	// fmt.Scan(&x, &y)
	x = 0
	y = 0

	operasi := (22 / y) / (7 + 2 * x) + x * y
	fmt.Print("Tim Dosen Alpro_1 ", operasi)

}
